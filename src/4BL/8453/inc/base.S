.include "macros.S"

# =================================================================================
# Extend the loader (used to be 0x6000, that could cause problems though)
# =================================================================================

MAKEPATCH 0x0000000C
0:
	.long 0x5C00
9:

# =================================================================================
# remove CE hash check
# (branch conditionally -> branch)
# =================================================================================
MAKEPATCH 0xC38
0:
    .long 0x48000014
9:

# =================================================================================
# remove CE validity check
# (branch conditionally -> nop)
# =================================================================================
MAKEPATCH 0xCDC
0:
    .long 0x60000000
9:

# =================================================================================
# remove CE validity check
# (branch conditionally -> nop)
# =================================================================================
MAKEPATCH 0xCEC
0:
    .long 0x60000000
9:

# =================================================================================
# remove CE validity check
# (branch conditionally -> nop)
# =================================================================================
MAKEPATCH 0xCF8
0:
    .long 0x60000000
9:

# =================================================================================
# remove CE validity check
# (branch conditionally -> nop)
# =================================================================================
MAKEPATCH 0xD04
0:
    .long 0x60000000
9:

# =================================================================================
# remove CF signature check
# (branch conditionally -> nop)
# =================================================================================
MAKEPATCH 0x7E4
0:
    .long 0x60000000
9:

# =================================================================================
# remove CF/CG hash check
# (li r3, 0 -> li r3, 1)
# =================================================================================
MAKEPATCH 0x19F8
0:
    .long 0x38600001
9:

# =================================================================================
# remove unpaired bit from bootloader flags
# =================================================================================
MAKEPATCH 0x00000E3C
0:
    MAKEBRANCH 0x00005B50
9:

MAKEPATCH 0x00005B50
0:
    or    %r11, %r11, %r10    # Original instruction
    xori  %r11, %r11, 1       # Modify flags
    MAKEBRANCH 0x00000E40     # Return to offset + 4
9:

# =================================================================================
# some kinda salt, helps stop CB/kernel from freezing after glitched hash check
# =================================================================================
# That's all folks!

MAKEPATCH 0x5BEF
0:
    .ascii "That's all folks!"
9: